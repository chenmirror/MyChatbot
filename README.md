# Chatbot SSE D

This is a simple chatbot  using Server-Sent Events (SSE).

## Project Structure

- `backend/`: Koa.js backend for handling chat and SSE.
- `frontend/`: React frontend for the chat interface.
